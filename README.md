# 🚀 Daily Grow: Парсер отзывов с Яндекс Карт

[![Laravel](https://img.shields.io/badge/Laravel-12.x-red)](https://laravel.com)
[![Vue.js](https://img.shields.io/badge/Vue.js-3.x-brightgreen)](https://vuejs.org)
[![Docker](https://img.shields.io/badge/Docker-Ready-blue)](https://www.docker.com)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

**Daily Grow** — это готовое production-решение для автоматического сбора, анализа и отображения отзывов о компании с Яндекс Карт. Проект построен на Laravel, Vue.js (Inertia) и включает в себя мощный парсер на Puppeteer, работающий с асинхронными очередями.

### ✨ Возможности

*   **🤖 Интеллектуальный парсер:** Использует Puppeteer для обхода защиты Яндекса и AJAX-запросы в контексте браузера для быстрого сбора данных.
*   **📊 Сбор по аспектам:** Автоматически определяет все аспекты (кухня, атмосфера, обслуживание и т.д.) на странице организации и собирает отзывы по каждому из них.
*   **⚙️ Гибкая стратегия сбора:** Для каждого аспекта собирается до 600 отзывов (негативные -> позитивные). При необходимости подключаются общие ранкинги (`by_relevance`, `by_time`, `by_rating`) для максимально полного охвата.
*   **📈 Прогресс в реальном времени:** Процесс парсинга отображается в админ-панели
*   **📱 Современный интерфейс:** Админ-панель на Vue 3 + Inertia.js с Tailwind CSS для удобного просмотра и управления отзывами.
*   **🐳 Полная контейнеризация:** Весь проект, включая все сервисы (PHP, Nginx, Redis, очередь, парсер), упакован в Docker и готов к развертыванию одной командой.

### 🛠️ Стек технологий

*   **Backend:** Laravel 12, PHP 8.4
*   **Frontend:** Vue.js 3, Inertia.js, Tailwind CSS, Vite
*   **База данных:** MariaDB/MySQL
*   **Очереди и кэш:** Redis
*   **Парсер:** Puppeteer (Node.js)
*   **Контейнеризация:** Docker, Docker Compose
*   **Управление процессами:** Supervisor

### 📁 Структура проекта
```
daily-grow/
├── docker/
│   ├── Dockerfile
│   ├── nginx.conf
│   └── supervisord.conf
├── src/
│   ├── app/
│   ├── bootstrap/
│   ├── config/
│   ├── database/
│   ├── public/
│   ├── resources/
│   ├── routes/
│   ├── storage/
│   ├── puppeteer/
│   │   └── worker.cjs
│   ├── artisan
│   ├── composer.json
│   ├── .env.example
│   └── package.json
├── docker-compose.yml
└── README.md
```

### ⚙️ Как это работает (Кратко)
Сбор данных: В админ-панели запускается задача на парсинг. Laravel Job кладет задание в очередь Redis.

Парсинг: Node.js воркер (puppeteer-worker), управляемый Supervisor, забирает задание из очереди. Он открывает страницу организации в headless-браузере, извлекает все необходимые параметры (CSRF-токен, ID сессии, список аспектов) и выполняет серию точных AJAX-запросов к API Яндекс Карт.

Прогресс: Промежуточные результаты (порции отзывов по 50 штук) и прогресс сохраняются в Redis.

Сохранение: Laravel Queue Worker (laravel-queue) периодически проверяет наличие новых данных в Redis и сохраняет их в базу данных. Статистика по общему прогрессу отображается в админ-панели в реальном времени.

```bash
git clone https://github.com/Lumerc/daily-grow.git
cd daily-grow
```
Далее нам нужен файл daily-grow/src/.env.example. Здесь мы пропишем адрес сайта, порт, https/http, логин пароль к админке

```
daily-grow/
└── src/
    └──.env.example
```

### 📄 Пример файла окружения (`.env.example`)

```env
APP_NAME="Daily Grow"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=laravel
DB_USERNAME=root
DB_PASSWORD=root

REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

ADMIN_EMAIL=admin@dailygrow.ru
ADMIN_PASSWORD=please_change_me
ADMIN_NAME=Administrator
APP_URL=http://localhost:8082
```

ADMIN_EMAIL=admin@dailygrow.ru // логин
ADMIN_PASSWORD=please_change_me // пароль
ADMIN_NAME=Administrator  //имя администратора

## Для HTTP
APP_ENV=local //окружение для разработки
APP_URL=http://localhost:8082 //или другой URL сайта + порт

Порт который ожидает контейнер 8082, что также можно отредактировать в файле docker-compose.yml

## Для HTTPS
APP_ENV=production //окружение для продакшена
APP_URL=https://localhost //или другой URL сайта, порт понятное дело 443 можно не писать

### Далее создаём окружение
```
docker-compose up --build
```
Желательно не ставить флаг -d (фоновый режим), так как разворачивание проекта идёт не только в dockerfile, но и после создания образов. Будут выводиться логи и порой, многие команды будут работать внушительное время (например, npm install puppeteer-core)
Также пройдёт миграция, а в конце запустятся демоны nginx, puppeteer, php-fpm, laravel-queue, поэтому, когда появятся сообщения примерно такие

```
yandex_parser  | 2026-02-17 14:01:08,999 INFO success: laravel-queue entered RUNNING state, process has stayed up for > than 1 seconds (startsecs)
yandex_parser  | 2026-02-17 14:01:09,000 INFO success: nginx entered RUNNING state, process has stayed up for > than 1 seconds (startsecs)
yandex_parser  | 2026-02-17 14:01:09,000 INFO success: php-fpm entered RUNNING state, process has stayed up for > than 1 seconds (startsecs)
yandex_parser  | 2026-02-17 14:01:09,000 INFO success: puppeteer-worker entered RUNNING state, process has stayed up for > than 1 seconds (startsecs)
```

Это говорит о том, что все демоны готовы к тому, чтобы мы зашли по адресу ЗАПОЛНЕННЫЙ-ВЫШЕ-URL/admin и заполнили логин + пароль, что также указали выше

Парсинг идёт довольно долго (минут 30, чтобы лишний раз не напрягать яндекс), можно следить за прогрессом 0% -> 100% на странице настроек

У меня не получилось собрать все отзывы (только 1160 из 1185). Покидать страницу можно, вся работа проходит в фоне.

